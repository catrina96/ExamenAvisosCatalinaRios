import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tuempresa.examenavisoscatalinarios',
  appName: 'ExamenAvisosCatalinaRios',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
